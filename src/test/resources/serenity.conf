serenity {
project.name = "OrangeHRM Employees - Serenity Screenplay"
driver = "chrome"

  webdriver {
    driver = chrome
    autowired = true
  }

    take.screenshots = FOR_EACH_ACTION
    logging = VERBOSE
}

environments {
  default {
    webdriver.base.url = "https://opensource-demo.orangehrmlive.com"
  }
}